{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/lz.spec.schema.json",
  "title": "Landing Zone spec",
  "description": "Generated from lz_spec.schema v2.2. The python schema is authoritative; regenerate after any schema change (python -m lz_pipeline.tools.gen_jsonschema).",
  "type": "object",
  "required": [
    "format",
    "schema_version",
    "customer",
    "sheets"
  ],
  "properties": {
    "format": {
      "type": "string",
      "pattern": "^lz-spec-ir/"
    },
    "schema_version": {
      "type": "string"
    },
    "customer": {
      "type": "string"
    },
    "source": {
      "type": "string"
    },
    "provenance": {
      "type": "object",
      "description": "Questionnaire lineage stamped by `lzctl assess`. When source_type is 'questionnaire', `lzctl build` requires the named decisions file to exist beside the spec with matching customer and assessment_id.",
      "properties": {
        "source_type": {
          "type": "string",
          "enum": [
            "questionnaire"
          ]
        },
        "decisions_file": {
          "type": "string"
        },
        "assessment_id": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "decision_set_sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "Hash of the immutable decision set (ref/state/question/targets/default_if_silent - never resolution). build exits 3 when the manifest no longer matches."
        },
        "decision_count": {
          "type": "integer",
          "description": "diagnostic; the hash is authoritative"
        },
        "customer": {
          "type": "string"
        }
      },
      "required": [
        "source_type",
        "decisions_file",
        "assessment_id",
        "decision_set_sha256",
        "customer"
      ]
    },
    "sheets": {
      "type": "object",
      "properties": {
        "Global": {
          "type": "object",
          "description": "Workspace-wide inputs. Do not store AK/SK credentials in this workbook. Provide them through $env:HW_ACCESS_KEY and $env:HW_SECRET_KEY.",
          "properties": {
            "Settings": {
              "type": "object",
              "description": "Required for every environment.",
              "properties": {
                "home_region": {
                  "type": "string",
                  "description": "Primary Huawei Cloud deployment region ID."
                },
                "state_bucket_name": {
                  "type": "string",
                  "description": "Globally unique OBS bucket name for Terraform state. Created by 00-bootstrap."
                }
              },
              "additionalProperties": true
            },
            "MasterDefaultTags": {
              "type": "array",
              "description": "Default tags applied to supported resources in the management account and all member accounts through cross-account provider aliases. Each row is one key/value pair. Add or remove rows as needed, for example owner, costcenter, bu, env, or compliance. Organization account resources created in 01-foundation are intentionally untagged because that environment does not use provider default tags.",
              "items": {
                "type": "object",
                "properties": {
                  "Key": {
                    "description": "Tag key."
                  },
                  "Value": {
                    "description": "Tag value."
                  }
                },
                "additionalProperties": true
              }
            }
          },
          "additionalProperties": true
        },
        "01_Foundation": {
          "type": "object",
          "description": "Environment 01-foundation (module 1): organization, OUs, accounts, and Identity Center bootstrap.",
          "properties": {
            "Settings": {
              "type": "object",
              "description": "Organization-level settings. The core logging and security accounts are required and are defined in the CoreAccounts table.",
              "properties": {
                "identity_center_alias": {
                  "type": "string",
                  "description": "Optional Identity Center alias. Leave blank to use no alias."
                },
                "cross_account_agency_name": {
                  "type": "string",
                  "description": "Trust agency name created automatically in each provisioned account."
                },
                "create_enterprise_project": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Create the bootstrap enterprise project. Module 8 creates the additional cost-center enterprise projects."
                },
                "enterprise_project_name": {
                  "type": "string",
                  "description": "Name of the bootstrap enterprise project."
                },
                "enforce_tag_keys_scp": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Add a ninth guardrail to the consolidated SCP in 04-perimeter. It blocks create actions when request tag keys are outside the TagPolicies TagKey set. Tag policies alone report non-compliance; this control enforces it."
                }
              },
              "additionalProperties": true
            },
            "EnabledPolicyTypes": {
              "type": "array",
              "description": "Organization root policy types. Only the two rows below are valid. Toggle Enabled and do not add additional rows.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Policy type name (do not edit)"
                  },
                  "Enabled": {
                    "type": [
                      "boolean",
                      "string"
                    ],
                    "description": "TRUE to enable at org root"
                  },
                  "Description": {
                    "description": "Effect (do not edit)"
                  }
                },
                "additionalProperties": true
              }
            },
            "OrganizationalUnits": {
              "type": "array",
              "description": "Each row creates an organizational unit. Delete rows you do not need. Parent='root' or blank places the OU under the organization root. Parent='<OUName>' nests it under another OU in this table. Cycles fail during planning. Do not add a row for the organization root.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "OU name (also used as the Terraform map key)"
                  },
                  "Parent": {
                    "description": "'root' / blank = under org root; or another OU's Name"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "CoreAccounts": {
              "type": "array",
              "description": "Required core accounts for logging and security. At minimum, include lz-infra and lz-security.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Account name (used as map key)"
                  },
                  "Email": {
                    "description": "UNIQUE email per Huawei requirement"
                  },
                  "OU": {
                    "description": "OU name from OrganizationalUnits; '' = root"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "WorkloadAccounts": {
              "type": "array",
              "description": "Workload accounts. Each row creates an account with a cross-account access agency. Delete rows you do not need.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Account name"
                  },
                  "Email": {
                    "description": "UNIQUE email"
                  },
                  "OU": {
                    "description": "OU placement (must match an OU Name or be blank for root)"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "TrustedServices": {
              "type": "array",
              "description": "Huawei Organizations trusted services. Toggle Enabled for each service. DelegatedAdmin is an account name from CoreAccounts or WorkloadAccounts. Leave it blank to keep administration in the management account. Add rows only for supported services added after this template was generated, and verify the service principal first.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Service principal — exactly as listed by the Huawei discovery data source"
                  },
                  "Enabled": {
                    "type": [
                      "boolean",
                      "string"
                    ],
                    "description": "TRUE to enable org-wide integration"
                  },
                  "DelegatedAdmin": {
                    "description": "Optional — account Name to be the delegated admin for this service; blank = none"
                  },
                  "Description": {
                    "description": "What enabling this service does"
                  }
                },
                "additionalProperties": true
              }
            },
            "TagPolicies": {
              "type": "array",
              "description": "Custom tag policies in addition to the default policy. Each row defines one required tag key. TagValue is a comma-separated list of allowed values; leave it blank to allow any value while enforcing key presence and casing. Scope is a comma-separated list of '<service>:<resourceType>'; leave it blank to apply to all taggable resource types. Tag policies report non-compliance. Use the module 4 SCP guardrail when you need hard enforcement.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Policy name (lowercase-hyphenated; becomes the Huawei policy resource name)"
                  },
                  "TagKey": {
                    "description": "Tag key (lowercase — keys are normalized to lowercase to match the applied tags)"
                  },
                  "TagValue": {
                    "description": "Comma-separated allowed values; blank = any value allowed"
                  },
                  "Scope": {
                    "description": "Comma-separated '<service>:<resourceType>' list; blank = all services"
                  }
                },
                "additionalProperties": true
              }
            }
          },
          "additionalProperties": true
        },
        "02_Finance": {
          "type": "object",
          "description": "Environment 02-finance (module 8): cost-center enterprise projects.",
          "properties": {
            "Settings": {
              "type": "object",
              "description": "",
              "properties": {
                "enable_multi_ep": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Create cost-center enterprise projects for 02-finance."
                }
              },
              "additionalProperties": true
            },
            "CostCenters": {
              "type": "array",
              "description": "Cost-center enterprise projects in addition to the bootstrap enterprise project from module 1. Accounts lists where each project is created. Use comma-separated account names from 01_Foundation, 'master' for the management account, or 'all' for the management account and every 01_Foundation account. Leave blank for the management account only.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "EP name (map key within each account)"
                  },
                  "Description": {
                    "description": "Purpose"
                  },
                  "EnterpriseProjectType": {
                    "description": "prod | dev | uat"
                  },
                  "Accounts": {
                    "description": "Target accounts: names, 'master', or 'all'. Blank = master."
                  }
                },
                "additionalProperties": true
              }
            }
          },
          "additionalProperties": true
        },
        "03_Identity": {
          "type": "object",
          "description": "Environment 03-identity (module 2): Identity Center configuration and the per-account IAM baseline.",
          "properties": {
            "Settings": {
              "type": "object",
              "description": "",
              "properties": {
                "enable_identity_center_content": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Create Identity Center users, groups, and permission sets in the management account."
                },
                "enable_iam_baseline": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Apply per-account IAM hardening, including password, login, operation-protection policies, and agencies."
                },
                "session_duration": {
                  "type": "string",
                  "description": "ISO 8601 duration for permission-set sessions."
                },
                "ic_min_password_length": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Minimum password length for Identity Center local users."
                },
                "ic_password_max_age_days": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Password expiry period, in days, for Identity Center local users."
                },
                "ic_mfa_required": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Require MFA for Identity Center users."
                },
                "iam_session_timeout_minutes": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Per-account IAM console session timeout."
                },
                "iam_lockout_duration_minutes": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Per-account IAM lockout duration after failed sign-in attempts."
                }
              },
              "additionalProperties": true
            },
            "Groups": {
              "type": "array",
              "description": "IC workforce groups.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Group name"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "Users": {
              "type": "array",
              "description": "Identity Center users. GroupNames is a comma-separated list and each value must match a group in the Groups table.",
              "items": {
                "type": "object",
                "properties": {
                  "UserName": {
                    "description": "Login name"
                  },
                  "DisplayName": {
                    "description": "Display name"
                  },
                  "FamilyName": {
                    "description": "Family name"
                  },
                  "GivenName": {
                    "description": "Given name"
                  },
                  "Email": {
                    "description": "Email"
                  },
                  "GroupNames": {
                    "description": "Comma-separated group names"
                  }
                },
                "additionalProperties": true
              }
            },
            "PermissionSets": {
              "type": "array",
              "description": "Permission sets. SystemPolicies is a comma-separated list of Huawei Cloud v2012 policy names.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "PS name"
                  },
                  "Description": {
                    "description": "Purpose"
                  },
                  "SessionDuration": {
                    "description": "ISO-8601; '' = inherit Settings.session_duration"
                  },
                  "SystemPolicies": {
                    "description": "Comma-separated v2012 system policy names"
                  }
                },
                "additionalProperties": true
              }
            },
            "AccountAssignments": {
              "type": "array",
              "description": "Bindings between groups, permission sets, and accounts. AccountName must match an account defined in module 1.",
              "items": {
                "type": "object",
                "properties": {
                  "AccountName": {
                    "description": "Account name (from M1 CoreAccounts/WorkloadAccounts)"
                  },
                  "GroupName": {
                    "description": "Group name from Groups"
                  },
                  "PermissionSet": {
                    "description": "PS name from PermissionSets"
                  }
                },
                "additionalProperties": true
              }
            },
            "AppPermissionSets": {
              "type": "array",
              "description": "Application-scoped Identity Center permission sets. CustomPolicy contains a complete v5.0 custom identity policy with enterprise project IDs. See EP_Scoping for the supported pattern. Account and EnterpriseProjects document the target member account and enterprise projects. Use one row per permission set and leave the table empty if application-scoped permission sets are not required.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Permission set name (also the resource key), e.g. App-Admin"
                  },
                  "Account": {
                    "description": "Member account (Name from 01_Foundation) whose enterprise projects scope the policy"
                  },
                  "EnterpriseProjects": {
                    "description": "Comma-separated EP names in Account to scope g:EnterpriseProjectId to"
                  },
                  "SessionDuration": {
                    "description": "ISO-8601 session duration (default PT8H)"
                  },
                  "Description": {
                    "description": "Permission set description"
                  },
                  "CustomPolicy": {
                    "description": "Complete v5.0 custom identity policy JSON, attached verbatim (EP IDs baked in; see EP_Scoping sheet). Blank = permission set created without a custom policy."
                  }
                },
                "additionalProperties": true
              }
            },
            "RegisteredRegions": {
              "type": "array",
              "description": "Regions where Identity Center can issue session credentials. Leave blank to use the home region only.",
              "items": {
                "type": "object",
                "properties": {},
                "additionalProperties": true
              }
            },
            "ServiceAgencies": {
              "type": "array",
              "description": "Per-account service agencies that allow Huawei Cloud services to act in the account. Policies is a comma-separated list.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Agency name"
                  },
                  "Description": {
                    "description": "Purpose"
                  },
                  "DelegatedService": {
                    "description": "service.<NAME> (e.g. service.CTS)"
                  },
                  "Policies": {
                    "description": "Comma-separated system policy names"
                  },
                  "AllResources": {
                    "type": [
                      "boolean",
                      "string"
                    ],
                    "description": "TRUE for org-wide; FALSE requires ProjectName"
                  },
                  "ProjectName": {
                    "description": "Required when AllResources=FALSE"
                  }
                },
                "additionalProperties": true
              }
            }
          },
          "additionalProperties": true
        },
        "04_Perimeter": {
          "type": "object",
          "description": "Module 4: data-perimeter service control policies, identity guardrails, predefined tags, and Config setup. Feeds the 04-perimeter environment.",
          "properties": {
            "SCPs": {
              "type": "array",
              "description": "Landing Zone service control policies. Each row represents one policy. Enabled creates the policy, and Enforce promotes it from dry-run to enforced. Fill only the settings that apply to that policy and leave the remaining cells blank. Do not rename the Policy keys.",
              "items": {
                "type": "object",
                "properties": {
                  "Policy": {
                    "description": "Policy key (fixed — do not rename)"
                  },
                  "Enforce": {
                    "type": [
                      "boolean",
                      "string"
                    ],
                    "description": "TRUE = attach the SCP (LIVE/enforced); FALSE = created but not attached (staged/inert)"
                  },
                  "Name": {
                    "description": "Explicit SCP name"
                  },
                  "AllowedOrgPath": {
                    "description": "RAM-share / RMS-aggregation only: '<org-id>/<root-id>/*'. Blank = derive from org_id + root_id."
                  },
                  "MandatoryTags": {
                    "description": "require_mandatory_tags only: required tag keys (e.g. Project,Owner,Environment,BU)."
                  },
                  "ExceptionTagKey": {
                    "description": "deny_public_obs only: exception RESOURCE-tag key. BLANK = no exception (public denied outright). Create the bucket private + tagged first, THEN its ACL/policy can be made public."
                  },
                  "ExceptionTagValue": {
                    "description": "deny_public_obs only: exception tag value (default 'approved')."
                  },
                  "AdminPrincipalURNs": {
                    "description": "protect_cts_tracker only: principal URN(s) allowed to modify CTS. BLANK = no exception."
                  },
                  "AllowedRegions": {
                    "description": "deny_outside_allowed_region only: allowed region code(s) e.g. ap-southeast-3."
                  }
                },
                "additionalProperties": true
              }
            },
            "PredefinedTags": {
              "type": "array",
              "description": "TMS predefined tag dictionary applied to the management account and every module 1 account. Values is a comma-separated list; leave it blank for free-form values. New accounts added in module 1 receive the same predefined tags when the environment is rebuilt.",
              "items": {
                "type": "object",
                "properties": {
                  "Key": {
                    "description": "Tag key"
                  },
                  "Values": {
                    "description": "Comma-separated allowed values; '' = free-form"
                  }
                },
                "additionalProperties": true
              }
            },
            "ConfigSetup": {
              "type": "object",
              "description": "Config service (RMS) org setup. Config is a GLOBAL service: org-wide resources (aggregator, conformance packs below) run from ONE account — set ConfigAdminAccount to the account they deploy on, typically the service.RMSMultiAccountSetup delegated admin (Sheet 01 TrustedServices). build_envs generates a provider alias + module call for that account. Leave ConfigAdminAccount blank to skip all Config setup. The resource recorder is the per-account 'turn Config on' tracker (records to an OBS bucket / SMN); the org aggregator pulls every member's compliance data into the admin account for a single-pane view.",
              "properties": {
                "config_admin_account": {
                  "type": "string",
                  "description": "Account name from Sheet 01 that hosts organization-wide Config. Leave blank to skip Config setup. This should match the delegated administrator for RMS multi-account setup."
                },
                "enable_config_recorder": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Create the Config resource recorder in the administrator account. A recorder is required before rules or conformance packages can evaluate resources."
                },
                "recorder_agency_name": {
                  "type": "string",
                  "description": "Name of the recorder trust agency created by Terraform. Use a name different from the system rms_tracker_trust_agency to avoid a naming conflict."
                },
                "create_recorder_agency": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "TRUE creates the v5 trust agency. FALSE references an existing agency using recorder_agency_name."
                },
                "recorder_bucket_name": {
                  "type": "string",
                  "description": "OBS bucket used for Config recorder snapshots. Required when enable_config_recorder is TRUE."
                },
                "create_recorder_bucket": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "TRUE creates a private, versioned, KMS-encrypted bucket with public access blocked. FALSE uses the existing bucket named in recorder_bucket_name."
                },
                "recorder_bucket_region": {
                  "type": "string",
                  "description": "Region of the Config recorder OBS bucket. Leave blank to use home_region."
                },
                "recorder_all_supported": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "TRUE records all supported resource types. FALSE restricts recording to a custom set that is not exposed in this workbook."
                },
                "recorder_smn_topic_urn": {
                  "type": "string",
                  "description": "Optional SMN topic URN for Config change notifications. Leave blank to use OBS delivery only."
                },
                "enable_config_aggregator": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Create an organization-type resource aggregator in the administrator account for multi-account compliance visibility."
                },
                "aggregator_name": {
                  "type": "string",
                  "description": "Name of the organization resource aggregator."
                }
              },
              "additionalProperties": true
            },
            "ConfigConformancePacks": {
              "type": "array",
              "description": "Config (RMS) conformance packages provide detective controls. They continuously evaluate resources and report violations, complementing the preventive SCPs above. Set Enabled=TRUE for each package you want to deploy organization-wide. TemplateKey identifies the predefined package template; leave it blank to resolve the template by name from the Config catalog.",
              "items": {
                "type": "object",
                "properties": {
                  "Package": {
                    "description": "Conformance package (fixed name — do not rename)"
                  },
                  "Category": {
                    "description": "Cloud best practice | Industry standard | Security config guide"
                  },
                  "Name": {
                    "description": "Org conformance-pack resource name (the one name it carries org-wide). Blank = slug of Package."
                  },
                  "TemplateKey": {
                    "description": "Predefined template key; blank = resolve by name from the templates data source"
                  },
                  "Vars": {
                    "description": "Optional parameter overrides as 'key=value' (comma-separated); values are JSON-encoded as strings. Needed where a template default is invalid — e.g. PCI DSS needs 'trackBucket=<cts-audit-bucket>' (default '' fails minLength 3). Blank = template defaults."
                  },
                  "Description": {
                    "description": "What the package evaluates"
                  },
                  "ExcludedAccounts": {
                    "description": "Accounts to EXEMPT from this pack - they skip the rule rollout entirely (fewer resources, and avoids a member whose RFS deployment keeps failing, e.g. Huawei edge-WAF 418 on read-back). Account Names from Sheet 01 OR raw 32-hex domain IDs, comma-separated. The management (master) account is always excluded automatically. Blank = no extra exemptions."
                  }
                },
                "additionalProperties": true
              }
            }
          },
          "additionalProperties": true
        },
        "05_Network": {
          "type": "object",
          "description": "Environment 05-network (module 3): hub and spoke networking, including ER, VPCs, CFW, NAT, ELB, EIPs, and RAM sharing. Hub and spoke resources deploy together in one apply, and spoke attachments connect to the ER created in the same run.",
          "properties": {
            "Settings": {
              "type": "object",
              "description": "Hub-wide settings and the landing zone enterprise project. ER and CFW have dedicated settings tables; NAT, ELB, and EIP support multiple instances.",
              "properties": {
                "enable_hub": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Create the hub network, including ER, VPCs, CFW, NAT, and ELB."
                },
                "enable_spoke": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Create spoke VPCs in workload accounts."
                },
                "hub_account": {
                  "type": "string",
                  "description": "Account name from module 1 where hub network resources are deployed. The environment assumes this account's OrganizationAccountAccessAgency."
                },
                "spoke_private_supernet": {
                  "type": "string",
                  "description": "Supernet covering all hub and spoke private CIDRs. The SNAT VPC receives a route for this supernet through the ER to provide a return path to spoke networks. The supernet must not overlap the SNAT VPC subnets."
                },
                "enterprise_project_name": {
                  "type": "string",
                  "description": "Landing zone enterprise project for the hub account. Supported hub resources are assigned to it. The value must match a CostCenters.Name in Sheet 02. Leave blank to use the default enterprise project."
                },
                "snat_vpc_attachment": {
                  "type": "string",
                  "description": "Hub ER VPC attachment that hosts the egress NAT gateway. The er-outbound route table sends 0.0.0.0/0 to this attachment after firewall inspection. Leave blank when no outbound default route is required."
                },
                "subnet_dns_servers": {
                  "type": "string",
                  "description": "DNS server IPs, maximum two, applied to every hub and spoke subnet through DHCP. Point these to the 08_DNS inbound resolver endpoint IPs when centralized hybrid DNS is used. Leave blank to use Huawei Cloud default DNS."
                },
                "enable_vpc_flow_logs": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Create a VPC flow log for every hub and spoke VPC, with a dedicated LTS group and stream named '<vpc>-flowlog'. Add corresponding LogConverge rows in 06_Observability to archive these logs. Apply 05-network before re-applying 06-observability so the source streams exist."
                },
                "flow_log_retention_days": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Hot LTS retention, in days, for each VPC flow-log group and stream."
                }
              },
              "additionalProperties": true
            },
            "HubVPCs": {
              "type": "array",
              "description": "Hub VPCs. Subnets are defined in HubSubnets and linked by VPCName. vpc-dmz is required when enable_hub is TRUE.",
              "items": {
                "type": "object",
                "properties": {
                  "VPCName": {
                    "description": "VPC name (vpc-dmz, vpc-access, vpc-shared)"
                  },
                  "CIDR": {
                    "description": "VPC CIDR"
                  }
                },
                "additionalProperties": true
              }
            },
            "HubSubnets": {
              "type": "array",
              "description": "Subnets linked to HubVPCs by VPCName. Availability zones are not pinned because Huawei Cloud places subnets automatically.",
              "items": {
                "type": "object",
                "properties": {
                  "VPCName": {
                    "description": "FK -> HubVPCs.VPCName"
                  },
                  "Name": {
                    "description": "Subnet name"
                  },
                  "CIDR": {
                    "description": "Subnet CIDR"
                  }
                },
                "additionalProperties": true
              }
            },
            "EnterpriseRouter": {
              "type": "object",
              "description": "The single hub Enterprise Router. Availability zones are defined in the ERAvailabilityZones table.",
              "properties": {
                "er_name": {
                  "type": "string",
                  "description": "Name of the hub Enterprise Router."
                },
                "er_asn": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Autonomous system number for the Enterprise Router."
                },
                "er_flow_log_name": {
                  "type": "string",
                  "description": "Name of the Enterprise Router flow log."
                },
                "er_auto_accept_shared_attachments": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Automatically accept ER attachments from principals that receive the shared ER through RAM."
                },
                "er_share_name": {
                  "type": "string",
                  "description": "Name of the RAM resource share for ER attachments."
                }
              },
              "additionalProperties": true
            },
            "ERAvailabilityZones": {
              "type": "array",
              "description": "Availability zones used by the Enterprise Router, one per row. Use the full Huawei Cloud AZ identifiers returned by the API, such as ap-southeast-3a and ap-southeast-3e.",
              "items": {
                "type": "object",
                "properties": {},
                "additionalProperties": true
              }
            },
            "HubERAttachments": {
              "type": "array",
              "description": "HUB ER VPC attachments — one row per hub-account attachment. Each joins a hub VPC to the ER via a subnet, and is AUTO-associated to the inbound RT + AUTO-propagated to the outbound RT (Settings.inbound/outbound_route_table); the VPC also auto-gets its default route to the ER (or NAT, for the SNAT VPC). AutoAddRoute=TRUE lets the ER auto-create the VPC-side route back to the ER. The egress attachment is named in Settings.snat_vpc_attachment. (Spoke attachments live in SpokeERAttachments.)",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Attachment name (auto-wired; Settings.snat_vpc_attachment target)"
                  },
                  "VPC": {
                    "description": "FK -> HubVPCs.VPCName"
                  },
                  "Subnet": {
                    "description": "Subnet name (from HubSubnets) carrying the attachment. Blank = VPC's first subnet."
                  },
                  "AutoAddRoute": {
                    "type": [
                      "boolean",
                      "string"
                    ],
                    "description": "TRUE = ER auto-creates the VPC-side return route (auto_create_vpc_routes)"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "CloudFirewall": {
              "type": "object",
              "description": "The single hub Cloud Firewall.",
              "properties": {
                "cfw_name": {
                  "type": "string",
                  "description": "Name of the hub Cloud Firewall."
                },
                "cfw_flavor": {
                  "type": "string",
                  "description": "standard | professional."
                },
                "cfw_ips_protection_mode": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "IPS protection mode for the hub firewall: 0 observe, 1 strict, 2 medium, or 3 loose. Leave blank to manage this setting in the console."
                },
                "cfw_ips_patch_enabled": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "IPS virtual patching for the hub firewall. Leave blank to manage this setting in the console."
                },
                "inspection_cidr_reservation": {
                  "type": "string",
                  "description": "CIDR used by Cloud Firewall for its ER-mode inspection attachment. It must not overlap any VPC CIDR."
                },
                "cfw_charging_mode": {
                  "type": "string",
                  "description": "Cloud Firewall billing mode: pay-per-use or subscription."
                },
                "cfw_period_unit": {
                  "type": "string",
                  "description": "Subscription period unit: month or year. Used only when cfw_charging_mode is subscription."
                },
                "cfw_period": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Subscription period count, for example 1. Used only when cfw_charging_mode is subscription."
                },
                "cfw_auto_renew": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Automatically renew the Cloud Firewall subscription at the end of the billing period."
                },
                "cfw_lts_log_enable": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Send Cloud Firewall logs to LTS. The hub module creates the log group and streams below."
                },
                "cfw_lts_log_group_name": {
                  "type": "string",
                  "description": "Name of the LTS log group created for Cloud Firewall logs."
                },
                "cfw_lts_traffic_stream_name": {
                  "type": "string",
                  "description": "LTS stream for Cloud Firewall traffic and flow logs. This stream is also used for the ER attachment flow log."
                },
                "cfw_lts_access_stream_name": {
                  "type": "string",
                  "description": "LTS stream for Cloud Firewall access logs."
                },
                "cfw_lts_attack_stream_name": {
                  "type": "string",
                  "description": "LTS stream for Cloud Firewall attack logs."
                }
              },
              "additionalProperties": true
            },
            "EIPs": {
              "type": "array",
              "description": "Elastic IPs, one per row. Each EIP has dedicated bandwidth. NAT and ELB resources reference EIPs by Name. BilledBy controls bandwidth metering.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "EIP name (referenced by SNATRules/DNATRules/ELBs)"
                  },
                  "Type": {
                    "description": "Public IP type: 5_bgp (dynamic BGP) | 5_sbgp (static BGP)"
                  },
                  "BilledBy": {
                    "description": "Bandwidth metering: bandwidth | traffic"
                  },
                  "BandwidthSize": {
                    "type": [
                      "integer",
                      "string"
                    ],
                    "description": "Bandwidth size (Mbit/s)"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "NATGateways": {
              "type": "array",
              "description": "Public NAT gateways in the hub, one per row. SNATRules and DNATRules reference the gateway by Name. A typical hub uses one NAT gateway in the DMZ VPC.",
              "items": {
                "type": "object",
                "properties": {
                  "Specification": {
                    "description": "Small | Medium | Large | Extra-large"
                  },
                  "Name": {
                    "description": "NAT gateway name (FK target for SNAT/DNAT rows)"
                  },
                  "VPC": {
                    "description": "FK -> HubVPCs.VPCName the NAT deploys into"
                  },
                  "Subnet": {
                    "description": "FK -> HubSubnets.Name the NAT deploys into"
                  }
                },
                "additionalProperties": true
              }
            },
            "SNATRules": {
              "type": "array",
              "description": "SNAT rules. CIDR is the source subnet translated to the named EIP through the named NAT gateway. Leave NATName blank when only one NAT gateway exists.",
              "items": {
                "type": "object",
                "properties": {
                  "NATName": {
                    "description": "FK -> NATGateways.Name (blank = sole NAT)"
                  },
                  "CIDR": {
                    "description": "Source CIDR"
                  },
                  "EIP": {
                    "description": "FK -> EIPs.Name"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "DNATRules": {
              "type": "array",
              "description": "DNAT rules that map a named EIP to an internal target. Leave NATName blank when only one NAT gateway exists.",
              "items": {
                "type": "object",
                "properties": {
                  "NATName": {
                    "description": "FK -> NATGateways.Name (blank = sole NAT)"
                  },
                  "EIP": {
                    "description": "FK -> EIPs.Name"
                  },
                  "ExternalPort": {
                    "type": [
                      "integer",
                      "string"
                    ],
                    "description": "External port"
                  },
                  "InternalIP": {
                    "description": "Internal target IP"
                  },
                  "InternalPort": {
                    "type": [
                      "integer",
                      "string"
                    ],
                    "description": "Internal port"
                  },
                  "Protocol": {
                    "description": "tcp | udp"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "ELBs": {
              "type": "array",
              "description": "Dedicated IPv4 load balancers in the hub, billed pay-per-use. AZ is a comma-separated list. EIP references an entry in EIPs for public access; leave it blank for an internal load balancer. IPAsBackend enables cross-VPC IP backends.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "ELB name"
                  },
                  "AZ": {
                    "description": "AZ ids, comma-separated (e.g. az1,az5)"
                  },
                  "VPC": {
                    "description": "FK -> HubVPCs.VPCName"
                  },
                  "FrontendSubnet": {
                    "description": "FK -> HubSubnets.Name for the ELB's IPv4 VIP"
                  },
                  "BackendSubnet": {
                    "description": "FK -> HubSubnets.Name for backend members (blank = none)"
                  },
                  "IPAsBackend": {
                    "type": [
                      "boolean",
                      "string"
                    ],
                    "description": "TRUE = allow IP-as-backend (cross-VPC backends)"
                  },
                  "EIP": {
                    "description": "FK -> EIPs.Name for public access (blank = internal-only)"
                  }
                },
                "additionalProperties": true
              }
            },
            "RAMSharePrincipals": {
              "type": "array",
              "description": "Principals that receive access to the shared hub ER. Use one account name from module 1, a raw 32-character account ID, or an OU ID per row. Leave the table empty to derive principals from the workload OU and workload accounts.",
              "items": {
                "type": "object",
                "properties": {},
                "additionalProperties": true
              }
            },
            "SpokeVPCs": {
              "type": "array",
              "description": "Spoke VPCs, typically one per workload account. Subnets are defined in SpokeSubnets and linked by VPCName, while ER attachments are defined in SpokeERAttachments. The baseline security group and flow log are derived from VPCName. The spoke provider applies Global default tags, and the Tags column can override individual keys. Include the full mandatory tag set when row-specific values are required.",
              "items": {
                "type": "object",
                "properties": {
                  "AccountName": {
                    "description": "Spoke account name (must match M1)"
                  },
                  "VPCName": {
                    "description": "VPC name"
                  },
                  "CIDR": {
                    "description": "VPC CIDR"
                  },
                  "Tags": {
                    "description": "Per-VPC tags, comma-delimited key=value (e.g. CostCenter=app,Owner=appteam). Also applied to the spoke ER attachment."
                  }
                },
                "additionalProperties": true
              }
            },
            "SpokeSubnets": {
              "type": "array",
              "description": "Subnets linked to SpokeVPCs by VPCName. Availability zones are not pinned. The first subnet in each VPC carries the spoke ER attachment. Subnet tags are set explicitly rather than inherited from Global default tags.",
              "items": {
                "type": "object",
                "properties": {
                  "VPCName": {
                    "description": "FK -> SpokeVPCs.VPCName"
                  },
                  "Name": {
                    "description": "Subnet name"
                  },
                  "CIDR": {
                    "description": "Subnet CIDR"
                  },
                  "Tags": {
                    "description": "Per-subnet tags, comma-delimited key=value (e.g. CostCenter=app,Tier=db)"
                  }
                },
                "additionalProperties": true
              }
            },
            "SpokeERAttachments": {
              "type": "array",
              "description": "SPOKE ER VPC attachments — one row per spoke VPC (created in the spoke's own member account). Mirrors HubERAttachments. Each is AUTO-associated to the inbound RT + AUTO-propagated to the outbound RT, and the spoke VPC auto-gets 0.0.0.0/0 -> ER. AutoAddRoute=TRUE lets the ER auto-create the VPC-side route. Use a distinct Name scheme (e.g. spoke-*) to tell hub vs spoke attachments apart. OMIT the row for a SpokeVPCs entry to create that spoke UNATTACHED (isolated sandbox: VPC/subnets/SG exist, no ER attachment/route — unreachable from the hub and other spokes).",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Attachment name (blank = att-<VPC>)"
                  },
                  "VPC": {
                    "description": "FK -> SpokeVPCs.VPCName"
                  },
                  "Subnet": {
                    "description": "Subnet name (from SpokeSubnets) carrying the attachment. Blank = VPC's first subnet."
                  },
                  "AutoAddRoute": {
                    "type": [
                      "boolean",
                      "string"
                    ],
                    "description": "TRUE = ER auto-creates the VPC-side return route (auto_create_vpc_routes)"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            }
          },
          "additionalProperties": true
        },
        "06_Observability": {
          "type": "object",
          "description": "Environment 06-observability (modules 6 and 7): CTS, OBS, KMS, and LTS for audit, plus SMN and Cloud Eye for operations.",
          "properties": {
            "AuditSettings": {
              "type": "object",
              "description": "",
              "properties": {
                "cts_admin_account": {
                  "type": "string",
                  "description": "Required. Delegated administrator account from 01_Foundation where the central audit resources are deployed, including the organization CTS tracker, audit bucket, KMS key, and CTS log group and stream."
                },
                "audit_retention_days": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Retention period for objects in the CTS audit bucket."
                },
                "audit_cold_after_days": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Number of days before CTS audit objects move to the COLD storage class. Use 0 to disable transition."
                },
                "lts_hot_retention_days": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Hot retention period for the CTS LTS log stream."
                },
                "audit_bucket_name": {
                  "type": "string",
                  "description": "Required. Globally unique CTS audit OBS bucket name. Supports the {account-name} token."
                },
                "kms_audit_alias": {
                  "type": "string",
                  "description": "Required. KMS alias for the audit bucket key. Supports {account-name}."
                },
                "cts_log_group_name": {
                  "type": "string",
                  "description": "CTS LTS log group name. Supports {account-name}."
                },
                "cts_log_stream_name": {
                  "type": "string",
                  "description": "CTS LTS log stream name. Supports {account-name}."
                },
                "kms_pending_days": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "KMS pending-deletion window. Production environments should use 30 days."
                },
                "audit_bucket_force_destroy": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Use with caution. TRUE allows Terraform to delete a non-empty audit bucket when the bucket name changes. This deletes stored audit logs and should remain FALSE unless a deliberate recreation is required."
                },
                "cts_no_transfer_accounts": {
                  "type": "string",
                  "description": "Comma-separated account names from 01_Foundation that receive a CTS tracker without OBS or LTS transfer. Use this only when local CTS visibility is required in addition to the central organization tracker. Exclude cts_admin_account. Leave blank when no account needs this."
                }
              },
              "additionalProperties": true
            },
            "LogAggregation": {
              "type": "object",
              "description": "Organization-wide LTS log aggregation. Member-account log streams converge into the delegated LTS administrator account, then transfer to an archive OBS bucket on a schedule. Hot retention is controlled by converged_retention_days and archive retention by archive_retention_days.",
              "properties": {
                "enable_log_aggregation": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Enable the log aggregation stack, including convergence, target log groups, archive bucket, and scheduled transfers. The administrator account is derived from the delegated LTS administrator configured in 01_Foundation."
                },
                "archive_bucket_name": {
                  "type": "string",
                  "description": "Globally unique archive OBS bucket name for LTS transfers. Supports {account-name}. Leave blank to use the generated default."
                },
                "kms_archive_alias": {
                  "type": "string",
                  "description": "KMS alias for the archive bucket key. Supports {account-name}. Leave blank to use the generated default."
                },
                "archive_retention_days": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Archive bucket object retention period, in days."
                },
                "archive_cold_after_days": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Number of days before archive objects move to the COLD storage class. Use 0 to disable transition."
                },
                "converged_retention_days": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "Hot retention period, in days, for converged LTS groups and streams."
                },
                "transfer_period": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "OBS transfer interval. Valid values depend on transfer_period_unit."
                },
                "transfer_period_unit": {
                  "type": "string",
                  "description": "OBS transfer interval unit: min or hour."
                },
                "archive_bucket_force_destroy": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Use with caution. TRUE allows Terraform to delete a non-empty archive bucket when its name changes. This deletes archived logs."
                }
              },
              "additionalProperties": true
            },
            "LogConverge": {
              "type": "array",
              "description": "NO INPUT NEEDED: leave EMPTY and every LZ-created stream is derived on build — the CTS stream (cts_admin_account), DNS query logs (08_DNS AccessLogs, dns_account), the CFW traffic/access/attack streams (hub_account) and one <vpc>-flowlog per VPC. Add rows only to CURATE (extra app-team streams, or to exclude a source) — a non-empty table is authoritative and fully replaces the derivation. Account is the SOURCE account name (from 01_Foundation); SourceGroup/SourceStream are LTS group/stream NAMES in that account (resolved to IDs via generated per-account lookups). TargetGroup blank = agg-<account>-<sourcegroup>. Rows whose Account IS the LTS admin transfer straight to the archive bucket (no converge).",
              "items": {
                "type": "object",
                "properties": {
                  "Account": {
                    "description": "SOURCE account name (from 01_Foundation) owning the log group"
                  },
                  "SourceGroup": {
                    "description": "LTS log group NAME in that account"
                  },
                  "SourceStream": {
                    "description": "LTS log stream NAME within SourceGroup"
                  },
                  "TargetGroup": {
                    "description": "Converged group name in the admin account. Blank = agg-<account>-<sourcegroup>."
                  },
                  "Description": {
                    "description": "What the stream carries"
                  }
                },
                "additionalProperties": true
              }
            },
            "OpsSettings": {
              "type": "object",
              "description": "",
              "properties": {
                "accounts": {
                  "type": "string",
                  "description": "Required. Comma-separated accounts from 01_Foundation that receive operational monitoring resources, or 'all'. Each account receives its own SMN topic, subscriptions, and one-click alarms."
                },
                "topic_name": {
                  "type": "string",
                  "description": "SMN topic name. Supports {account-name}."
                }
              },
              "additionalProperties": true
            },
            "Subscribers": {
              "type": "array",
              "description": "SMN subscribers. Email subscribers must confirm the subscription outside Terraform.",
              "items": {
                "type": "object",
                "properties": {
                  "Protocol": {
                    "description": "email | sms | http | https | functionstage | callnotify | dms"
                  },
                  "Endpoint": {
                    "description": "Address (email / phone / URL)"
                  }
                },
                "additionalProperties": true
              }
            },
            "OneClickNamespaces": {
              "type": "array",
              "description": "Cloud Eye one-click monitoring bundles. Toggle Enabled for each service. Enabled bundles notify the configured SMN topic and apply to existing and future resources for that service. EventEnabled controls event alarm rules.",
              "items": {
                "type": "object",
                "properties": {
                  "Namespace": {
                    "description": "Cloud Eye namespace (SYS.*)."
                  },
                  "EventEnabled": {
                    "type": [
                      "boolean",
                      "string"
                    ],
                    "description": "Include event alarms for this bundle."
                  },
                  "Description": {
                    "description": "Service the namespace covers."
                  }
                },
                "additionalProperties": true
              }
            }
          },
          "additionalProperties": true
        },
        "07_Security": {
          "type": "object",
          "description": "Environment 07-security: SecMaster in the security account, plus Basic Anti-DDoS for hub EIPs and a dedicated WAF in the hub DMZ.",
          "properties": {
            "Settings": {
              "type": "object",
              "description": "",
              "properties": {
                "security_account": {
                  "type": "string",
                  "description": "Account name from module 1 where the SecMaster workspace is deployed."
                },
                "secmaster_workspace_name": {
                  "type": "string",
                  "description": "Name of the SecMaster workspace."
                },
                "enable_hss": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "RESERVED - Host Security Service is not implemented; the flag is wired to the module but deploys nothing. Keep FALSE (LZR-033) and record the requirement as a decision."
                },
                "hss_quota_count": {
                  "type": [
                    "integer",
                    "string"
                  ],
                  "description": "RESERVED - see enable_hss. Number of HSS quotas to purchase once the service is implemented."
                },
                "enable_dbss": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "RESERVED - Database Security Service is not implemented; the flag is wired to the module but deploys nothing. Keep FALSE (LZR-033) and record the requirement as a decision."
                },
                "enable_member_workspaces": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Reserved for a future Pattern B upgrade."
                }
              },
              "additionalProperties": true
            },
            "SecMasterModules": {
              "type": "array",
              "description": "SecMaster functional modules to enable.",
              "items": {
                "type": "object",
                "properties": {},
                "additionalProperties": true
              }
            },
            "AlertRules": {
              "type": "array",
              "description": "Baseline SecMaster detection rules.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Rule name"
                  },
                  "Description": {
                    "description": "Purpose"
                  },
                  "Severity": {
                    "description": "tips | low | medium | high | fatal"
                  },
                  "RuleType": {
                    "description": "e.g. log"
                  },
                  "Query": {
                    "description": "Detection query"
                  }
                },
                "additionalProperties": true
              }
            },
            "AntiDDoS": {
              "type": "array",
              "description": "Basic (Cloud Native) Anti-DDoS traffic-cleaning per EIP — pay-per-use tuning of the free per-EIP protection; nothing is purchased and 'destroy' just resets the default threshold. EIP references a 05_Network EIPs row by Name (resolved from 05-network state; deployed into the hub account). AlarmTopic optionally names a 06_Observability SMN topic for cleaning alarms (resolved from 06-observability state; blank = no notification). CNAD Advanced / AAD need pre-purchased instances and are configured in console, not here.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Row name (unique)"
                  },
                  "EIP": {
                    "description": "FK -> 05_Network EIPs.Name (the protected EIP)"
                  },
                  "ThresholdMbps": {
                    "type": [
                      "integer",
                      "string"
                    ],
                    "description": "Traffic-cleaning threshold: 10|30|50|70|100|120|150|200|250|300|1000"
                  },
                  "AlarmTopic": {
                    "description": "SMN topic NAME from 06_Observability (in the hub account). Blank = no alarm."
                  }
                },
                "additionalProperties": true
              }
            },
            "WAF": {
              "type": "object",
              "description": "Dedicated WAF instance deployed in the hub DMZ VPC. Domains in the table below route through this WAF. VPC, subnet, and security-group names are resolved from the 05_Network state.",
              "properties": {
                "enable_waf": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Create the dedicated WAF instance, policy, and protected domains."
                },
                "waf_instance_name": {
                  "type": "string",
                  "description": "Name of the dedicated WAF instance."
                },
                "waf_specification_code": {
                  "type": "string",
                  "description": "waf.instance.professional (WI-500) | waf.instance.enterprise (WI-100)."
                },
                "waf_availability_zone": {
                  "type": "string",
                  "description": "Required when WAF is enabled. Availability zone for the WAF engine ECS."
                },
                "waf_vpc": {
                  "type": "string",
                  "description": "Required when WAF is enabled. HubVPCs name from 05_Network for the DMZ VPC."
                },
                "waf_subnet": {
                  "type": "string",
                  "description": "Required when WAF is enabled. HubSubnets name from 05_Network within the selected VPC."
                },
                "waf_policy_name": {
                  "type": "string",
                  "description": "Shared WAF protection policy attached to all configured domains."
                }
              },
              "additionalProperties": true
            },
            "WAFDomains": {
              "type": "array",
              "description": "Domains protected by the dedicated WAF. Origin is usually the private VIP of the hub ingress ELB or another backend reachable from the WAF VPC. CertificateId is required when ClientProtocol is HTTPS.",
              "items": {
                "type": "object",
                "properties": {
                  "Domain": {
                    "description": "Protected domain, e.g. app.example.com (or *.example.com)"
                  },
                  "ClientProtocol": {
                    "description": "Browser -> WAF: HTTP | HTTPS (default HTTP)"
                  },
                  "ServerProtocol": {
                    "description": "WAF -> origin: HTTP | HTTPS (default HTTP)"
                  },
                  "OriginAddress": {
                    "description": "Origin server IP/hostname (e.g. the ingress ELB VIP)"
                  },
                  "OriginPort": {
                    "type": [
                      "integer",
                      "string"
                    ],
                    "description": "Origin port (default 80)"
                  },
                  "CertificateId": {
                    "description": "WAF certificate ID (HTTPS only; blank for HTTP)"
                  }
                },
                "additionalProperties": true
              }
            }
          },
          "additionalProperties": true
        },
        "08_DNS": {
          "type": "object",
          "description": "Env 08-network-dns (DNS module): Cloud DNS — public + private zones with record sets, plus the hybrid resolver (inbound + outbound endpoints, outbound forwarding rules, query access logging). Applied AFTER 05-network: zones/endpoints/rules reference 05_Network VPCs and subnets by NAME (resolved from the 05-network state). All resources deploy into ONE account (Settings.dns_account). Needed Terraform resources: dns_zone, dns_recordset, dns_private_zone_associate, dns_endpoint, dns_resolver_rule, dns_resolver_rule_associate, dns_resolver_access_log.\nHOW PRIVATE DNS RESOLVES ORG-WIDE: a private zone is only visible to VPCs explicitly associated to it, and association is SAME-ACCOUNT only (the provider has no zone sharing). The LZ therefore uses the HUB-RESOLVER pattern: every hub + spoke subnet's DHCP hands out the INBOUND endpoint IPs (05_Network Settings.subnet_dns_servers), so all queries resolve through the resolver VPC — private zones, the on-prem forwarding rules (associate each ResolverRules row to the resolver VPC!), and public fall-through (Recursive=TRUE). Zone names are arbitrary domains (internal TLDs fine); records must sit at/under the zone apex — use additional zones for other apexes.",
          "properties": {
            "Settings": {
              "type": "object",
              "description": "Account and enterprise project used for DNS resources. The environment assumes the selected account's OrganizationAccountAccessAgency.",
              "properties": {
                "dns_account": {
                  "type": "string",
                  "description": "Account name from module 1 where DNS resources are deployed, usually the shared-services or hub account that owns the resolver VPC."
                },
                "enterprise_project_name": {
                  "type": "string",
                  "description": "Enterprise project for private zones, resolver endpoints, and forwarding rules. Must match a CostCenters.Name in Sheet 02. Leave blank to use the default enterprise project."
                }
              },
              "additionalProperties": true
            },
            "PublicZones": {
              "type": "array",
              "description": "Public DNS zones. Use one row per zone and toggle Enabled. Record sets are defined in RecordSets. Zone names must end with a trailing dot, for example 'example.com.'. Huawei Cloud provides the authoritative name servers, which must be delegated at your registrar.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Zone apex FQDN with trailing dot (e.g. example.com.)"
                  },
                  "Email": {
                    "description": "SOA contact email (blank = Huawei default)"
                  },
                  "TTL": {
                    "type": [
                      "integer",
                      "string"
                    ],
                    "description": "Zone SOA TTL in seconds (blank = 300)"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "PrivateZones": {
              "type": "array",
              "description": "Private DNS zones (resolvable only inside associated VPCs). One row per zone; toggle Enabled. VPCs is a comma-separated list of VPC NAMES (05_Network hub or spoke VPCs) to associate — the FIRST is the zone's primary router (huaweicloud_dns_zone.router); the rest are attached via huaweicloud_dns_private_zone_associate. Name ends with a trailing dot. Records live in RecordSets keyed by Zone. Recursive = the proxy_pattern: FALSE (default) = AUTHORITY (the zone owns its whole namespace in-VPC; names you don't record return NXDOMAIN); TRUE = RECURSIVE (names not in the zone fall through to public DNS - use this to override only a few names under a broad domain, e.g. a private myhuaweicloud.com zone that overrides just one endpoint).",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Private zone FQDN with trailing dot (e.g. internal.example.)"
                  },
                  "VPCs": {
                    "description": "VPC names to associate (from 05_Network). First = primary router; rest via private_zone_associate."
                  },
                  "TTL": {
                    "type": [
                      "integer",
                      "string"
                    ],
                    "description": "Zone SOA TTL in seconds (blank = 300)"
                  },
                  "Recursive": {
                    "type": [
                      "boolean",
                      "string"
                    ],
                    "description": "TRUE = proxy_pattern RECURSIVE (unmatched names fall through to public); blank/FALSE = AUTHORITY"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "RecordSets": {
              "type": "array",
              "description": "DNS record sets for the public and private zones above. Use one row per record and toggle Enabled. Zone must match a PublicZones or PrivateZones name. Name is the record FQDN with a trailing dot. Records is a comma-separated list of values appropriate to the record type.",
              "items": {
                "type": "object",
                "properties": {
                  "Zone": {
                    "description": "FK -> PublicZones.Name or PrivateZones.Name"
                  },
                  "Name": {
                    "description": "Record FQDN with trailing dot, within the zone"
                  },
                  "Type": {
                    "description": "A | AAAA | CNAME | MX | TXT | NS | SRV | CAA"
                  },
                  "Records": {
                    "description": "Comma-separated record values"
                  },
                  "TTL": {
                    "type": [
                      "integer",
                      "string"
                    ],
                    "description": "Record TTL in seconds (blank = 300)"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "ResolverEndpoints": {
              "type": "array",
              "description": "Hybrid-DNS resolver endpoints (huaweicloud_dns_endpoint), one row each; toggle Enabled. Direction = 'inbound' (lets on-premises resolvers query Huawei private zones — point on-prem forwarders at the endpoint IPs) or 'outbound' (lets cloud VPCs forward queries out via ResolverRules). VPC + Subnets name where the endpoint's resolver IPs live. Huawei REQUIRES >=2 resolver IPs: either list >=2 Subnets (ideally different AZs for HA), OR list 1 Subnet and >=2 IPs. One ip_addresses block is created per IP (or per subnet when IPs is blank).",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Endpoint name"
                  },
                  "Direction": {
                    "description": "inbound | outbound"
                  },
                  "VPC": {
                    "description": "FK -> 05_Network VPC name hosting the endpoint subnets"
                  },
                  "Subnets": {
                    "description": "Subnet name(s) within VPC (>=1). Use >=2 (different AZs) for HA, or 1 with >=2 IPs."
                  },
                  "IPs": {
                    "description": "Optional fixed IPs. Provide >=2 here to satisfy the min when using a single subnet; blank = auto-assign (needs >=2 subnets)."
                  }
                },
                "additionalProperties": true
              }
            },
            "ResolverRules": {
              "type": "array",
              "description": "Outbound resolver forwarding rules. Use one row per rule and toggle Enabled. Queries under DomainName are forwarded through the named outbound endpoint to TargetIPs. VPCs is a comma-separated list of VPC names associated with the rule. DomainName must end with a trailing dot.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Resolver rule name"
                  },
                  "Endpoint": {
                    "description": "FK -> ResolverEndpoints.Name (must be Direction=outbound)"
                  },
                  "DomainName": {
                    "description": "Domain to forward, trailing dot (e.g. corp.onprem.)"
                  },
                  "TargetIPs": {
                    "description": "Comma-separated target DNS server IPs (on-prem/external)"
                  },
                  "VPCs": {
                    "description": "VPC names (from 05_Network) to associate the rule with"
                  }
                },
                "additionalProperties": true
              }
            },
            "AccessLogs": {
              "type": "array",
              "description": "DNS resolver query logging to LTS. Use one row per configuration and toggle Enabled. LTSGroup and LTSStream are created for query logs. VPCs is a comma-separated list of VPCs whose resolver queries are logged.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Label for this access-log config (informational)"
                  },
                  "LTSGroup": {
                    "description": "LTS log group name to write query logs to"
                  },
                  "LTSStream": {
                    "description": "LTS log stream (topic) name to write query logs to"
                  },
                  "VPCs": {
                    "description": "VPC names (from 05_Network) whose DNS queries are logged"
                  }
                },
                "additionalProperties": true
              }
            }
          },
          "additionalProperties": true
        },
        "09_CFW": {
          "type": "object",
          "description": "Env 09-network-cfw (CFW module): Cloud Firewall rule plane on the hub firewall created in 05-network — object groups (IP address groups, app/network domain name groups, service groups), internet-border rules (EIP/NAT ACL rules + black/white lists) and VPC-border rules (protection ACL rules + black/white lists). Applied AFTER 05-network: the firewall instance ID is read from the 05-network state and its protect-object IDs (0=internet / north-south, 1=VPC / east-west) are resolved via the cfw_firewalls data source. Runs in Settings.cfw_account (the hub account). Required Terraform resources: cfw_address_group (+_member), cfw_domain_name_group, cfw_service_group (+_member), cfw_acl_rule, cfw_black_white_list.",
          "properties": {
            "Settings": {
              "type": "object",
              "description": "Account that hosts the hub Cloud Firewall. This must match 05_Network Settings.hub_account. The environment assumes the account's OrganizationAccountAccessAgency and reads the firewall ID from 05-network state.",
              "properties": {
                "cfw_account": {
                  "type": "string",
                  "description": "Account name from module 1 that owns the hub Cloud Firewall. This must match 05_Network Settings.hub_account."
                },
                "enable_anti_virus": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Enable antivirus protection for the internet protected object across supported protocols, with blocking action."
                },
                "enable_reverse_shell_defense": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Set reverse-shell advanced IPS rules on the internet protected object to enabled and blocking. Re-applying Terraform reasserts this setting."
                },
                "alarm_topic_name": {
                  "type": "string",
                  "description": "SMN topic name in the Cloud Firewall account for firewall alarm notifications. Required when any alarm toggle below is enabled."
                },
                "enable_attack_alarm": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Notify the alarm topic for CRITICAL and HIGH attack detections, at all times and for every occurrence."
                },
                "enable_traffic_alarm": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Notify the alarm topic when firewall bandwidth utilization exceeds 80%."
                },
                "enable_eip_unprotected_alarm": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Notify the alarm topic when an EIP is not protected by the firewall."
                },
                "enable_threat_intel_alarm": {
                  "type": [
                    "boolean",
                    "string"
                  ],
                  "description": "Notify the alarm topic for CRITICAL and HIGH threat-intelligence detections."
                }
              },
              "additionalProperties": true
            },
            "AddressGroups": {
              "type": "array",
              "description": "User-defined IP address groups. Use one row per group and toggle Enabled. Border selects the internet or VPC protected object. Members is a comma-separated list of IP addresses, CIDRs, or ranges.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Address group name"
                  },
                  "Border": {
                    "description": "internet | vpc — which protected object the group lives on. USE internet: groups bound to the vpc object work in rules but are INVISIBLE in the console object-group list (confirmed live 2026-07-08); rules on either border accept internet-bound groups"
                  },
                  "AddressType": {
                    "description": "ipv4 | ipv6 (default ipv4)"
                  },
                  "Members": {
                    "description": "Comma-separated IPs / CIDRs / ranges"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "DomainGroups": {
              "type": "array",
              "description": "Domain name groups. Use one row per group and toggle Enabled. Type can be application for HTTP/HTTPS/TLS-SNI matching or network for domain-to-IP resolution. Border selects the protected object. Domains is a comma-separated list.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Domain group name"
                  },
                  "Border": {
                    "description": "internet | vpc"
                  },
                  "Type": {
                    "description": "application | network"
                  },
                  "Domains": {
                    "description": "Comma-separated domain names (e.g. *.example.com,api.example.com)"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "ServiceGroups": {
              "type": "array",
              "description": "User-defined service groups. Use one row per group and toggle Enabled. Members is a comma-separated list in 'protocol/srcport/dstport' format. Protocol may be tcp, udp, icmp, or icmpv6; ports may be a single value, a range, or 'any'.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Service group name"
                  },
                  "Border": {
                    "description": "internet | vpc. USE internet — vpc-bound groups are invisible in the console list (see AddressGroups.Border)"
                  },
                  "Members": {
                    "description": "Comma-separated 'protocol/srcport/dstport' entries"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "ACLRules": {
              "type": "array",
              "description": "CFW ACL rules (huaweicloud_cfw_acl_rule). One row per rule; toggle Enabled. Kind selects the protected object + rule type: 'eip' (internet border, EIP rule), 'nat' (internet border, NAT rule) or 'vpc' (VPC border, protection rule). Action = allow | deny. Source / Destination are comma-separated tokens: a bare IP/CIDR, 'addrgroup:<Name>' (AddressGroups), 'domaingroup:<Name>' (DomainGroups; destination only), or 'any'. Service is comma-separated: 'any', an inline 'protocol/srcport/dstport', 'svcgroup:<Name>' (ServiceGroups), or 'app:<APP>' (L7 application, e.g. app:HTTPS). Rules are added in row order; reorder in the console if precedence matters.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Rule name"
                  },
                  "Kind": {
                    "description": "eip | nat | vpc"
                  },
                  "Action": {
                    "description": "allow | deny"
                  },
                  "Source": {
                    "description": "Comma-separated: IP/CIDR | addrgroup:<Name> | any"
                  },
                  "Destination": {
                    "description": "Comma-separated: IP/CIDR | addrgroup:<Name> | domaingroup:<Name> | any"
                  },
                  "Service": {
                    "description": "Comma-separated: any | protocol/srcport/dstport | svcgroup:<Name> | app:<APP>"
                  },
                  "Status": {
                    "description": "enable | disable (default enable)"
                  },
                  "Description": {
                    "description": "Purpose"
                  },
                  "Direction": {
                    "description": "inbound | outbound — internet border (eip/nat) only; blank = nat->outbound, eip->inbound. Inbound rules cannot use domain groups (CFW.00400028). Ignored for vpc."
                  }
                },
                "additionalProperties": true
              }
            },
            "BlackWhiteLists": {
              "type": "array",
              "description": "Cloud Firewall allowlists and blocklists. Use one row per entry and toggle Enabled. Direction determines whether the source or destination address is matched. Port applies only to TCP and UDP entries.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Label (informational — the resource has no name)"
                  },
                  "Border": {
                    "description": "internet | vpc"
                  },
                  "ListType": {
                    "description": "blacklist | whitelist"
                  },
                  "Direction": {
                    "description": "source | destination"
                  },
                  "Protocol": {
                    "description": "tcp | udp | icmp | icmpv6 | any"
                  },
                  "AddressType": {
                    "description": "ipv4 | ipv6 | domain"
                  },
                  "Address": {
                    "description": "Single IP / CIDR / domain"
                  },
                  "Port": {
                    "description": "Destination port (tcp/udp only); blank otherwise"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            }
          },
          "additionalProperties": true
        },
        "10_VPN": {
          "type": "object",
          "description": "Environment 10-network-vpn: enterprise site-to-cloud VPN gateways, customer gateways, and IPsec connections. Apply after 05-network because gateway attachments are resolved from the 05-network state. The environment runs in Settings.vpn_account.",
          "properties": {
            "Settings": {
              "type": "object",
              "description": "Account hosting the VPN, usually the 05-network hub account. The environment assumes this account's OrganizationAccountAccessAgency.",
              "properties": {
                "vpn_account": {
                  "type": "string",
                  "description": "Account name from module 1 where VPN resources are deployed, usually the hub account."
                },
                "enterprise_project_name": {
                  "type": "string",
                  "description": "Enterprise project for VPN resources. Must match a CostCenters.Name in Sheet 02. Leave blank to use the default enterprise project."
                }
              },
              "additionalProperties": true
            },
            "Gateways": {
              "type": "array",
              "description": "S2C VPN gateways (huaweicloud_vpn_gateway). One row per gateway; toggle Enabled. Attachment = 'vpc' (bind to a 05_Network VPC) or 'er' (bind to the hub Enterprise Router). EITHER WAY, VPC + ConnectSubnet are REQUIRED: for vpc they are the gateway's vpc_id + connect_subnet; for er they are the access_vpc_id + access_subnet_id (the gateway's interconnection plane — Huawei rejects an ER gateway without one). NetworkType = public (2 EIPs are created at BandwidthSize Mbit/s) or private. HAMode = active-active | active-standby. AZs: LEAVE BLANK to auto-select 2 AZs that actually stock the chosen flavor+attachment (recommended — hardcoding AZs that don't offer the flavor causes 'VPN.0001: resource not enough'). Only set AZs to pin specific zones. ASN is the gateway BGP ASN. Flavor blank = API default (Professional1).",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Gateway name"
                  },
                  "Attachment": {
                    "description": "vpc | er"
                  },
                  "VPC": {
                    "description": "REQUIRED. 05_Network VPC name (vpc attach -> vpc_id; er attach -> access_vpc_id)"
                  },
                  "ConnectSubnet": {
                    "description": "REQUIRED. Subnet name within VPC (vpc attach -> connect_subnet; er attach -> access_subnet_id)"
                  },
                  "LocalSubnets": {
                    "description": "vpc attachment only: local CIDRs the gateway advertises to on-prem"
                  },
                  "NetworkType": {
                    "description": "public | private (default public)"
                  },
                  "HAMode": {
                    "description": "active-active | active-standby (default active-standby)"
                  },
                  "Flavor": {
                    "description": "Gateway flavor (e.g. Basic, Professional1); blank = API default"
                  },
                  "AZs": {
                    "description": "Blank = auto-select 2 valid AZs for flavor+attachment (recommended). Only fill to pin zones (e.g. ap-southeast-3a,ap-southeast-3b)."
                  },
                  "ASN": {
                    "type": [
                      "integer",
                      "string"
                    ],
                    "description": "Gateway BGP ASN (default 64512)"
                  },
                  "BandwidthSize": {
                    "type": [
                      "integer",
                      "string"
                    ],
                    "description": "public only: bandwidth (Mbit/s) for each created EIP"
                  },
                  "ERAssocRouteTable": {
                    "description": "er attach only: FIXED ER route table (er-inbound | er-outbound | er-hybrid) the gateway's ER attachment ASSOCIATES to — the table that steers traffic ARRIVING from on-prem (use er-hybrid so DC traffic is CFW-inspected). Blank = no association."
                  },
                  "ERPropRouteTable": {
                    "description": "er attach only: FIXED ER route table (er-inbound | er-outbound | er-hybrid) the attachment PROPAGATES into — where on-prem routes land (BGP-learned, or PeerSubnets for static tunnels; typically er-outbound). This is the ONLY way on-prem routes enter ER — static routes to VPN attachments are rejected (ER.04006105). Blank = no propagation."
                  },
                  "EIPChargeMode": {
                    "description": "public only: EIP billing mode - bandwidth (default) or traffic. FORCENEW on the gateway: to switch on a LIVE gateway, change billing in the EIP console first, then set this to match and refresh state - never apply the diff."
                  }
                },
                "additionalProperties": true
              }
            },
            "CustomerGateways": {
              "type": "array",
              "description": "Customer gateways representing on-premises VPN devices. Use one row per device and toggle Enabled. IP is the device's public IP. ASN is used when RouteMode is bgp. RouteMode can be static or bgp.",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Customer gateway name"
                  },
                  "IP": {
                    "description": "On-premises public IP"
                  },
                  "ASN": {
                    "type": [
                      "integer",
                      "string"
                    ],
                    "description": "On-prem BGP ASN (for RouteMode=bgp; default 65000)"
                  },
                  "RouteMode": {
                    "description": "static | bgp"
                  }
                },
                "additionalProperties": true
              }
            },
            "Connections": {
              "type": "array",
              "description": "IPsec VPN connections (huaweicloud_vpn_connection) — the tunnels binding a Gateway to a CustomerGateway. One row per connection; toggle Enabled. Gateway / CustomerGateway are FKs to the tables above. VPNType = policy | static | bgp. PeerSubnets is a comma-separated list of on-prem CIDRs. HARole = master | slave (which gateway EIP the tunnel binds to; active-standby gateways use master, active-active use both). PSK is the IPsec pre-shared key (SENSITIVE - written to the gitignored tfvars, never commit).",
              "items": {
                "type": "object",
                "properties": {
                  "Name": {
                    "description": "Connection name"
                  },
                  "Gateway": {
                    "description": "FK -> Gateways.Name"
                  },
                  "CustomerGateway": {
                    "description": "FK -> CustomerGateways.Name"
                  },
                  "VPNType": {
                    "description": "policy | static | bgp"
                  },
                  "PeerSubnets": {
                    "description": "On-prem CIDRs reachable over the tunnel"
                  },
                  "HARole": {
                    "description": "master | slave (default master)"
                  },
                  "PSK": {
                    "description": "IPsec pre-shared key (sensitive)"
                  }
                },
                "additionalProperties": true
              }
            }
          },
          "additionalProperties": true
        },
        "11_SGACL": {
          "type": "object",
          "description": "Env 11-network-sgacl (secgroups module 15): workload security groups + rules, one module call per member account named in SecurityGroups.Account (assume_role provider). Groups are created with delete_default_rules=true - NO implicit allows; every permit (including egress) is an explicit SGRules row. Security groups are region-scoped (no VPC binding); attaching them to ECS NICs is the workload/migration team's step. SG-to-SG references (Remote = sg:<Name>) work only within one account. The NetworkACLs / ACLRules tables are RESERVED for subnet network ACLs - not implemented yet; enabled rows there fail validation (LZR-031). Required Terraform resources: networking_secgroup, networking_secgroup_rule.",
          "properties": {
            "SecurityGroups": {
              "type": "array",
              "description": "Security groups created with default rules removed. Use one row per group and toggle Enabled. Account must match a 01_Foundation account name. Tags uses a comma-separated k=v list and should include the mandatory project, bu, owner, and env tags.",
              "items": {
                "type": "object",
                "properties": {
                  "Account": {
                    "description": "Owning member account name (01_Foundation)"
                  },
                  "Name": {
                    "description": "Security group name"
                  },
                  "Description": {
                    "description": "Purpose"
                  },
                  "Tags": {
                    "description": "k=v,k=v - mandatory tag set required by the tag SCP"
                  }
                },
                "additionalProperties": true
              }
            },
            "SGRules": {
              "type": "array",
              "description": "Security group rules (huaweicloud_networking_secgroup_rule). One row per rule; toggle Enabled. SG names a SecurityGroups row (rules inherit its account). Remote: a CIDR, sg:<Name> (another group in the SAME account) or self. Ports: single (443), range (5985-5986), list (80,443) or blank = all ports. Protocol tcp | udp | icmp | any (icmp ignores Ports). Groups have no default rules, so give every group an explicit egress row (standard: any/0.0.0.0/0 - CFW governs the destinations).",
              "items": {
                "type": "object",
                "properties": {
                  "SG": {
                    "description": "SecurityGroups.Name this rule belongs to"
                  },
                  "Direction": {
                    "description": "ingress | egress"
                  },
                  "Protocol": {
                    "description": "tcp | udp | icmp | any (default any)"
                  },
                  "Ports": {
                    "description": "443 | 5985-5986 | 80,443 | blank = all"
                  },
                  "Remote": {
                    "description": "CIDR | sg:<Name> (same account) | self"
                  },
                  "Action": {
                    "description": "allow | deny (default allow)"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "NetworkACLs": {
              "type": "array",
              "description": "RESERVED - subnet network ACLs (huaweicloud_vpc_network_acl) are not implemented yet. Keep this table empty: enabled rows fail validation (LZR-031). When implemented, ACLs will associate to spoke subnets as a second, subnet-level enforcement layer (stateful; unmatched traffic denied once associated).",
              "items": {
                "type": "object",
                "properties": {
                  "Account": {
                    "description": "Owning member account name"
                  },
                  "Name": {
                    "description": "Network ACL name"
                  },
                  "VPC": {
                    "description": "Spoke VPC name (05_Network)"
                  },
                  "Subnets": {
                    "description": "Subnet names to associate"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            },
            "ACLRules": {
              "type": "array",
              "description": "RESERVED - rules for NetworkACLs; not implemented yet. Keep empty (LZR-031).",
              "items": {
                "type": "object",
                "properties": {
                  "ACL": {
                    "description": "NetworkACLs.Name"
                  },
                  "Direction": {
                    "description": "ingress | egress"
                  },
                  "Action": {
                    "description": "allow | deny"
                  },
                  "Protocol": {
                    "description": "tcp | udp | icmp | any"
                  },
                  "Source": {
                    "description": "Source CIDR"
                  },
                  "SourcePorts": {
                    "description": "Source ports (blank = all)"
                  },
                  "Destination": {
                    "description": "Destination CIDR"
                  },
                  "DestPorts": {
                    "description": "Destination ports (blank = all)"
                  },
                  "Description": {
                    "description": "Purpose"
                  }
                },
                "additionalProperties": true
              }
            }
          },
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    }
  }
}
